{
  "name": "svhaui-helper",
  "private": true,
  "version": "1.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build:minify": "tsc && cross-env MINIFY=true vite build",
    "build:all": "rimraf dist && pnpm build && pnpm build:minify",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/node": "^25.0.8",
    "cross-env": "^10.1.0",
    "rimraf": "^6.1.2",
    "typescript": "^5.9.2",
    "vite": "^7.1.3",
    "vite-plugin-monkey": "^7.1.1"
  }
}