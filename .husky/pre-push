#!/bin/sh

echo "🔨 Running build check before push..."

# Chạy TypeScript check và build
pnpm run build

if [ $? -ne 0 ]; then
  echo ""
  echo "❌ Build failed! Please fix the errors before pushing."
  exit 1
fi

echo "✅ Build passed! Pushing..."
